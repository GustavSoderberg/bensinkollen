export const GasStation = (key, logo, name, price, lat, long) => { 
    return { 
        key: key,
        logo: logo,
        name: name, 
        price: price, 
        lat: lat,
        long: long 
    } 
}