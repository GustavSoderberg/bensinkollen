export const GasStation = (region, name, address, types, logo, lat, long) => { 
    return { 
        
        region: region,
        name: name, 
        address: address,
        types: types, 
        logo: logo,
        lat: lat,
        long: long 
    } 
}